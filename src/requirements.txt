# Core
torch>=2.1.0
transformers>=4.40.0

# Required by transformers
accelerate>=0.29.0
safetensors>=0.4.0
sentencepiece>=0.1.99     # Llama-2 tokenizer (BPE via SentencePiece)
protobuf>=3.20.0          # sentencepiece dependency

# HuggingFace auth (needed to download gated models like Llama-2)
huggingface-hub>=0.22.0
